// twilight — DOM entry point. M1 stub: re-exports the pure core; the style
// injection adapter and `tw` helper land with the engine (M2).
export { compile, compileOne, type CompileResult } from "./core/index.ts";
